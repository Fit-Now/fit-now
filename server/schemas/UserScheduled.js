

const typeDefs = `#graphql

type UserSchedule {
    _id: ID
    UserId: ID
    CoachId: ID
    SchduleId: ID
    startDate: String
    endDate: String
    roomChatId: ID
    LocationId: ID
    CategoryId: ID
}


`